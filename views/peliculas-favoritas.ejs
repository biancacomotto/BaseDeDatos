<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Películas Favoritas de <%= username %></title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

<div class="container mt-4">
    <h1>Películas Favoritas de <%= username %></h1>

    <% if (favoritos && favoritos.length > 0) { %>
        <ul class="list-group">
            <% favoritos.forEach(movie => { %>
                <li class="list-group-item">
                    <strong>Título:</strong>
                    <a href="/pelicula/<%= movie.movie_id %>"><%= movie.title %></a>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No hay películas favoritas para este usuario.</p>
    <% } %>

    <div class="mt-4">
        <a href="/users" class="btn btn-primary">Volver a Usuarios</a>
        <a href="/" class="btn btn-secondary ml-2">Volver al Inicio</a>
    </div>
</div>

</body>
</html>
