<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Películas de Usuario</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Asegúrate de tener una hoja de estilos si la necesitas -->
</head>
<body>
<header>
    <h1>Películas Favoritas</h1>
</header>
<main>
    <% if (peliculas.length > 0) { %>
        <table>
            <thead>
            <tr>
                <th>Título</th>
                <th>Puntuación</th>
                <th>Opinión</th>
            </tr>
            </thead>
            <tbody>
            <% peliculas.forEach(function(pelicula) { %>
                <tr>
                    <td><%= pelicula.title %></td>
                    <td><%= pelicula.rating %></td>
                    <td><%= pelicula.opinion %></td>
                </tr>
            <% }); %>
            </tbody>
        </table>
    <% } else { %>
        <p>No hay películas favoritas para este usuario.</p>
    <% } %>
</main>
<footer>
    <a href="/usuarios">Volver a la lista de usuarios</a>
</footer>
</body>
</html>
